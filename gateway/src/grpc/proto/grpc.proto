syntax = "proto3";

package grpc;

service GrpcMicroservice {
    rpc GetGrpcMethod (Data) returns (Data) {}
}

message Data {
    int32 age = 1;
    string name = 2;
    string job = 3;
    string type = 4;
}

